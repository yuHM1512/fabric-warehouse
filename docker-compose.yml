services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fabric_warehouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - fabric_warehouse_pg:/var/lib/postgresql/data

volumes:
  fabric_warehouse_pg:

